# Registry configuration lands here when packages start publishing (Phase 4+,
# GitHub Packages or npmjs depending on the @canvas scope outcome — see README).
# Note: onlyBuiltDependencies lives in pnpm-workspace.yaml under pnpm 10, not here.
