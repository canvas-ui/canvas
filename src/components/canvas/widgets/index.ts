// Importing this module registers all built-in widgets as a side effect.
// Add a new widget by creating its file (with its own registerWidget call)
// and importing it here.
import './DocumentsTableWidget'
import './ClockWidget'
import './RecentDocumentsWidget'
import './NoteWidget'
